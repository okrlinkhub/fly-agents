# Changelog

## 0.1.3

- Fix lookup mismatch between `agentKey` and `tenantId/userId` for agent secrets, with fallback and canonical realignment.
- Add `companySecrets` secure storage and APIs (encrypted at rest) to provide tenant-level defaults.
- Improve integration UX readiness for consumer admin pages relying on consistent target user selection.

## 0.0.0

- Initial release.
